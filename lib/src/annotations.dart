
/// Annotation to mark a class as a command handler
/// This annotation is used to generate the registration of the command handlers
class CommandHandler {
  const CommandHandler();
}

/// Annotation to mark a class as a query handler
/// This annotation is used to generate the registration of the query handlers
class QueryHandler {
  const QueryHandler();
}

/// Annotation to mark a class as a request handler
/// This annotation is used to generate the registration of the request handlers
class RequestHandler {
  const RequestHandler();
}

/// Annotation to generate the mediator registration handlers
class MediatorInit {
  const MediatorInit();
}
